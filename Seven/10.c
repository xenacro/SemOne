#include <stdio.h>
int main(){
	float a,b,c;
	printf("Enter (a,b,c) for equation of type : ax + by + c ");
	scanf("%f %f %f",&a,&b,&c);
	printf("point of intersection of line with x-axis %.2f",7/a);
	return 0;
}